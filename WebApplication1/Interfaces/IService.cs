namespace WebApplication1.Interfaces
{
    public interface IService
    {
        decimal Add(decimal itemA, decimal itemB);

        decimal Minus(decimal itemA, decimal itemB);

        decimal Times(decimal itemA, decimal itemB);

        decimal Divided(decimal itemA, decimal itemB);

    }
}
